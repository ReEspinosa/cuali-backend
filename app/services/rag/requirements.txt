pdfplumber>=0.11.0
chromadb>=0.5.0
openai>=1.30.0
rank_bm25>=0.2.2
tqdm>=4.66.0

# Solo necesario si usas EMBEDDING_PROVIDER=local (instala también pytorch,
# es una descarga grande). Si vas a usar la API de OpenAI, puedes omitirla.
# sentence-transformers>=3.0.0
